<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wojtekkurylo.quizapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name=".ResultsQuestionActivity"/>
        <activity android:label="@string/app_name" android:name=".FourthQuestionActivity"/>
        <activity android:label="@string/app_name" android:name=".ThirdQuestionActivity"/>
        <activity android:label="@string/app_name" android:name=".SecondQuestionActivity"/>
        <activity android:label="@string/app_name" android:name=".FirstQuestionActivity"/>
        <activity android:label="@string/app_name" android:name="InstActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>